<br/>
<%= render 'layouts/navfront' %>

<div id="courses" class="transitions-enabled">
  <% @courses.each do |course| %>
    <div class="box panel panel-default">
      <div class="course-image">
        <%= link_to course do %>
          <%= image_tag course.feature_image.url(:medium) %>
          <p class="desc step-progress"><%= course.steps.where(:completed => true).size %> / <%= course.steps.size %></p>
        <% end %>
      </div>
      
      <!--
        <div class="progress progress-striped">
          <div class="progress-bar progress-bar-success" role="progressbar" aria-valuenow="60" aria-valuemin="0" aria-valuemax="100" style="width: 10%">
          </div>
        </div>-->
      
      <div class="panel-body text-center">
        <p class="desc"><%= course.name %></p>

        <!--<div class="course-author">
          <%= gravatar_tag course %>
          Chris Mikelson
        </div>-->
        <!--<p class="desc"><%= course.description %></p>
        <p><%= course.completed %></p>-->

          
          <!--&nbsp;
          <%= link_to course, :class => "btn btn-default" do %>
          <i class="fa fa-youtube-play"></i>&nbsp;
          Watch Trailer
          <% end %>
          <%= link_to course, :class => "btn btn-default" do %>
          <i class="fa fa-list"></i>&nbsp;
          View Transcript
          <% end %>-->
        
        
      </div>
    </div>
  <% end %>
</div>

   